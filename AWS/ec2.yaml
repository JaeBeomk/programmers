sudo passwd root
- ***********

sudo su - passwd
- *****
sudo add user beomzh
- *****

sudo apt update

sudo apt install tomcat9
- web server 

sudo apt install net-tools
- netstat
- netstat -nplt (확인)

aws 콘솔 -> 보안그룹 -> 인바운드 규칙 -> 8080포트를 열어줘야 8080접속가능

crome ec2 IPV4주소:8080 

tomcat 삭제시 --purge 옵션을 주어야 다 지워짐
sudo apt --purge remove tomcat9

ubuntu 버전 확인 명령어
lsb_release -a

버전에 맞게 apt 다운로드
https://launchpad.net/ubuntu/+ppas


tomcat port 변경시 etc/tomcat9/server.xml의 port를 변경하고
보안그룹의 인바운드 규칙도 변경해줘야한다
