// 가끔 문법을 찾아서 사용해 보자

// if 문 사용
function 조건(){
    if( 조건식 ) {
        // 참일 때 수행할 구문
      } else {
        //거짓일 때 실행될 구문
      }
}

// 문자열 찾기
function indexof(){
  var a="test";
  a.indexOf("t"); // 0
  a.indexOf("a"); // -1
  a.includes("t"); // true
  a.includes("a"); // false
}